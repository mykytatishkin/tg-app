TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
MINI_APP_URL=https://your-ngrok-url.ngrok-free.dev
JWT_SECRET=your_jwt_secret
# Stage: local = test mode (shows "in test" badge in app), other = production
STAGE=local
DB_HOST=46.21.250.43
DB_PORT=5432
DB_USER=tgapp
DB_PASSWORD=tgapp_secret
DB_NAME=tgapp
# For migrating data to production: set DB_HOST (and DB_USER, DB_PASSWORD, DB_NAME) to the target server, then run: node scripts/import-json-to-db.mjs ./data.json

# Backup: local (folder synced with OneDrive), onedrive (Microsoft Graph API), or email (send .sql to address)
BACKUP_DESTINATION=local
# For local: path to folder (e.g. OneDrive sync folder)
BACKUP_LOCAL_PATH=/path/to/OneDrive/Backups/tg-app
# Optional: timezone for daily cron (e.g. Europe/Kyiv)
# BACKUP_TZ=Europe/Kyiv
# Set to false if backend runs inside Docker (then use DB_HOST=postgres service name)
# BACKUP_USE_DOCKER=true

# For onedrive: Azure app client id/secret and refresh_token (from OAuth2 once)
# ONEDRIVE_CLIENT_ID=
# ONEDRIVE_CLIENT_SECRET=
# ONEDRIVE_REFRESH_TOKEN=

# For email: recipient and SMTP (optional auth)
# BACKUP_EMAIL_TO=you@work.com
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=backup@tg-app
